# Decision Log

## 2026-09-25 · 创新方向选择（/omp:ideate）
- **选定**：I6 主线，加上 I5 和 I1 两个组件（用户选择了推荐组合）。
- **降为可选组件**：
  - I3（快照分歧门控的 KL 加 DDRL 式锚定）：新颖 2 / 可行 4 / 影响 3。偏工程，单独成文不够。
  - I4（可学习性相机课程）：3 / 4 / 2。README 已把它列为后续模块（H4）。
- **暂不做**：I2（Sneaky AnyView 对抗找漏洞）：4 / 2 / 4。要多训练一个 RL 策略，r_GT 只在 Kubric 或 C_b=C_a 时能拿到。如果 I1 效果好，可以作为后续扩展。
- 用户在评估阶段要求加入"单纯的双向提升"（I6），它随后被选为主线。

## 2026-09-25 · README 审阅后修订
- **§5.1 编辑训练**：冻结、平移、交换物体得不到精确的几何标签。改为分成两类：(a) 退化只用于诊断；(b) 只有可严格推导的变换或重新渲染可以用于训练，并降为可选模块。删除"编辑视频但标签不变"的对照组。
- **§3.5**：尺度只用 t₀ 时刻的正深度点拟合；t ≠ t₀ 时不因 z 为负而惩罚。分段 advantage 降为后续消融。
- **§3.2**：重新锚定后的 world2cam 求逆，得到的不是原始世界坐标下的 T_wc。奖励用原始相机，显式保存 T_anchor。
- **§3.4**：40 帧和 48 帧的 clip 分开组成 microbatch。
- **§5**：所有字段继承公共掩码 M_anchor。
- **措辞**：H2 改为"当前配置未支持"；编辑训练改为"检验是否降低相关失效"。
- **查新**：标注为未独立核查。
